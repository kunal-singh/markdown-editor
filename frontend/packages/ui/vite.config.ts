// Stub vite config — required for shadcn CLI framework detection.
// This package has no build step; Vite runs only in app/.
// Stub vite config — required for shadcn CLI framework detection.
// This package has no build step; Vite runs only in app/.
import { defineConfig } from "vite";
export default defineConfig({});
